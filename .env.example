# 이 파일을 .env로 복사한 뒤 실제 값을 채워 넣으세요.
# .env 파일 자체는 절대 GitHub 등에 커밋하지 마세요 (.gitignore에 이미 포함되어 있습니다).

ANTHROPIC_API_KEY=sk-ant-여기에_실제_키_입력

# 선택사항 — 서버가 사용할 포트 (기본값 8787)
PORT=8787

# 선택사항 — 사용할 모델 (기본값 claude-sonnet-4-6)
ANTHROPIC_MODEL=claude-sonnet-4-6
